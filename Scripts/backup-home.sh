#!/bin/bash

# Backup local files (incuding cloud syned) to an alternate location.
# Some backup software will not touch cloud storage, so this
# script can copy those files to a non-synced location, allowing
# backup software to archive to another computer, NAS, or cloud storage.
#
# todo
